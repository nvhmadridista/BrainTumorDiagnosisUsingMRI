# Deploy Folder

This folder is for storing trained model files used for deployment.

## Usage

- Place your model files here (e.g., `model.pth`, `model.pt`).
- Do NOT commit model files to version control.
